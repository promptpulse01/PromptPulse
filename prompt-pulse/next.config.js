/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['img.clerk.com', 'pixner.net','res.cloudinary.com','cdn.shopify.com']
    }
}

module.exports = nextConfig
