import {type} from "os"

type Shop = {
    id: string
    name: string
    description: string
    shopProductsType: string
    avatar: string
    userId: string
    createdAt: string
    updatedAt: string
}